package montotone.com.github.www.basic.Basic.array

import org.junit.Test

class TestArray {
    @Test
    fun testArray() {
        var a = arrayOf(1, 2, 3);
        var b = Array(3, { i -> (i * 2) })
        println(a[0])

        for(int in b){
            println(int)
        }

    }
}